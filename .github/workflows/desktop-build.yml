name: Desktop packages

on:
  workflow_dispatch:

jobs:
  package:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y rpm fakeroot
      - run: npm ci
      - run: npm run check
      - run: npm run desktop:make
      - uses: actions/upload-artifact@v4
        with:
          name: frontend-editor-${{ runner.os }}-${{ runner.arch }}-unsigned
          path: desktop-dist/make/**
          if-no-files-found: error

# CI artifacts are deliberately marked unsigned. Promotion to a release requires
# Authenticode/Developer ID signing or a signed Linux package repository.
